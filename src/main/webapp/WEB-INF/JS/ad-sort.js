"use strict"

//VARIABLES
let sortByTitleBtn = document.querySelector("#sort-title");
let sortByPriceBtn = document.querySelector("#sort-price");
let allAdTitles = document.querySelectorAll(".ad-link");

let allAdTitles = document.querySelectorAll(".ad-link");
let adArray = [];

//FUNCTIONS
function createAdsObjects() {

};

function sortAdsByTitle(e) {

}


//EVENTS
sortByTitleBtn.addEventListener('click', (e) => {
    sortAdsByTitle(e);
})